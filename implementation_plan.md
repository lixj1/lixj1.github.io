# 变更概述

本次在现有文档站点基础上继续做第三轮扩展，优先补齐提示词页下一批高频专题，仍保持单个 HTML 文件交付，避免引入额外框架与构建工具。

独立变更如下：

1. [MODIFY] index.html
   - 保留现有左侧导航与文档站骨架。
   - 在“提示词分享”页内继续新增下一批第一层级专题。
   - 延续现有子导航结构，补齐更多高频团队场景模板。
   - 继续复用现有原生 JavaScript 路由，使新增专题可切换和定位。
   - 进一步压缩首页顶部引导文案，使首页更接近正式站点入口。
   - 压缩左侧导航说明文字，减少侧栏的解释感。
   - 压缩品牌说明和提示词页顶部说明，减少长句叙述。
   - 移除侧栏底部说明，并统一压短专题开头说明文案。
   - 保持现有视觉语言一致，避免新增内容破坏信息层级。

2. [NEW] walkthrough.md
   - 作为本轮改造完成后的交付说明文档。

# 理解摘要

- 当前项目只有一个静态页面 [index.html](index.html)，内容已包含“团队大模型使用指南”和“AI 模型对比”。
- 你希望把它升级成发布在 GitHub Pages 上的团队内部经验分享文档站。
- 目前第一页内容来源于 [llm_comparison_notes.md](llm_comparison_notes.md)，核心是指导团队配置环境。
- 第二页是“提示词分享”页面，后续会持续增加更多分层内容。
- “项目文件清理”已经是提示词分享页下的第一个层级内容。
- 你刚刚再次确认“按顺序来”，意味着继续沿着提示词分享页的专题扩容往下推进。
- 这条提示词的目标是清理当前文件夹下的历史项目代码，只保留一键启动所需程序、data，以及与 1316-305 项目相关的必要文件。
- 你希望页面中顺带体现推荐 skill：planning-with-files。
- 你希望额外告诉团队如何使用这个 skill，并说明你当前采用的是 .copilot/skills 目录结构。

# 假设

- 假设仍然只维护一个 [index.html](index.html)，通过原生 JavaScript 切换内容区和子层级内容。
- 假设你希望保留现有配图 step1.png 到 step4.png，并继续用于“团队大模型使用指南”页面。
- 假设新增页面内容先由我补齐一个适合团队内部使用的初版结构，后续你可再改文案。
- 假设本轮优先再新增三个高频团队场景专题，作为提示词页继续扩容的下一批内容。
- 假设页面主要在桌面端展示，但移动端也需要可用。
- 假设不新增后端、不引入打包器、不新增 npm 依赖。

# 决策记录

- 已确认首页是“团队大模型使用指南（入门必读）”。
- 已确认第二页为“提示词分享”。
- 已确认“项目文件清理”不再作为独立顶层页面，而是归到“提示词分享”页下作为第一层级内容。
- 已确认后续工作按顺序推进：先扩充提示词页，再补首页站点说明。
- 已完成上一批专题扩展后，当前继续优先补提示词页的更多高频专题。

# 提议变更

## [MODIFY] index.html

计划修改点：

1. 页面骨架
   - 新增整体外层布局容器，如 `app-shell`。
   - 左侧加入 `aside` 导航区，右侧加入 `main` 内容区。
   - 移除当前仅适合单页文档的居中大白卡模式，改为更接近“知识库 / 手册”式布局。

2. 导航交互
   - 顶层导航包含“首页”和“提示词分享”两个入口。
   - “提示词分享”页内增加多个子层级导航，首个子项为“项目文件清理”。
   - 点击导航时，切换当前 section 的显示状态。
   - 导航高亮当前页面与当前子层级。
   - 移动端下将左侧导航调整为顶部卡片式导航，避免遮挡正文。

3. 内容拆分
   - 首页：沿用当前“团队大模型使用指南（入门必读）”与模型对比内容，并更贴近团队文档站语境。
   - 提示词分享：保留提示词知识页结构，继续扩充更多场景化模板。
   - 项目文件清理：作为提示词分享页的第一层级内容，提供可复制的整理提示词、保留范围、排除范围与执行建议。
   - 新增专题 2：需求梳理与任务拆解。
   - 新增专题 3：旧项目接手与代码读盘。
   - 新增专题 4：报错定位与根因分析。
   - 新增专题 5：接口联调与模拟数据生成。
   - 新增专题 6：前端页面重构。

4. 视觉优化
   - 调整字体方案，避免继续依赖过于普通的 Inter 单字体方案。
   - 保留渐变、卡片、磨砂玻璃语言，但减少当前内联样式比例。
   - 提升层次感、站点感和左侧导航辨识度。

## [NEW] walkthrough.md

计划内容：

- 记录最终改造范围。
- 说明导航结构、页面拆分、子层级结构与交互逻辑。
- 记录验证方式与后续扩展点。

# 验证计划

自动验证：

- 检查 HTML 结构是否完整，避免标签嵌套错误。
- 检查导航点击是否能正确切换“首页 / 提示词分享”两个顶层页面。
- 检查“提示词分享”页内多个子层级是否都能正确定位和高亮。
- 检查移动端宽度下导航是否不会遮挡正文。

手动验证：

- 打开 [index.html](index.html)，确认侧栏导航可见。
- 依次点击“首页 / 提示词分享”，确认内容切换正确。
- 在“提示词分享”中检查各个专题内容块、推荐 skill 和提示词代码块是否显示正常。
- 检查首页新增的维护说明、更新说明和页脚信息是否显示正常。
- 检查首页中的 4 张配图显示是否正常。
- 检查页面在深色模式和浅色模式下观感是否可接受。