# 变更概述

本次在现有文档站点基础上继续调整提示词页内容顺序与专题主题，仍保持单个 HTML 文件交付，避免引入额外框架与构建工具。

独立变更如下：

1. [MODIFY] index.html
   - 保留现有左侧导航与文档站骨架。
   - 将“旧项目接手与代码读盘”专题替换为新的 MedReview 列名兼容改造专题。
   - 将新的 MedReview 专题调到提示词页第一位，并设为默认打开项。
   - 继续复用现有原生 JavaScript 路由，使调整后的专题顺序、高亮与定位保持一致。

2. [MODIFY] walkthrough.md
   - 同步记录本轮专题替换与顺序调整结果。

# 理解摘要

- 当前项目只有一个静态页面 [index.html](index.html)，内容已包含“团队大模型使用指南”和“AI 模型对比”。
- 你希望把它升级成发布在 GitHub Pages 上的团队内部经验分享文档站。
- 目前第一页内容来源于 [llm_comparison_notes.md](llm_comparison_notes.md)，核心是指导团队配置环境。
- 第二页是“提示词分享”页面，后续会持续增加更多分层内容。
- 你现在希望把“旧项目接手与代码读盘”专题替换成一条新的 MedReview 专项提示词。
- 新专题聚焦于“受试者”与“参与者”列名的全局兼容改造，用于指导实际代码改造任务。
- 你要求该专题放到提示词页最前面，成为默认展示的第一项。

# 假设

- 假设仍然只维护一个 [index.html](index.html)，通过原生 JavaScript 切换内容区和子层级内容。
- 假设你希望保留现有配图 step1.png 到 step4.png，并继续用于“团队大模型使用指南”页面。
- 假设页面仍维持当前 3 个专题结构，只替换其中一个专题的主题与顺序。
- 假设页面主要在桌面端展示，但移动端也需要可用。
- 假设不新增后端、不引入打包器、不新增 npm 依赖。

# 决策记录

- 已确认首页是“团队大模型使用指南（入门必读）”。
- 已确认第二页为“提示词分享”。
- 已确认“项目文件清理”仍保留在提示词页中。
- 已确认“旧项目接手与代码读盘”需要被新的 MedReview 列名兼容改造专题替换。
- 已确认新的 MedReview 专题需要放在提示词页第一位。

# 提议变更

## [MODIFY] index.html

计划修改点：

1. 页面骨架
   - 新增整体外层布局容器，如 `app-shell`。
   - 左侧加入 `aside` 导航区，右侧加入 `main` 内容区。
   - 移除当前仅适合单页文档的居中大白卡模式，改为更接近“知识库 / 手册”式布局。

2. 导航交互
   - 顶层导航继续保留“首页”和“提示词分享”两个入口。
   - “提示词分享”页内首个子项改为新的 MedReview 列名兼容改造专题。
   - 调整默认 hash 路由对应的默认专题，使首次进入提示词页时直接打开新专题。
   - 保持现有子导航高亮与定位逻辑可用。

3. 内容拆分
   - 首页：不改。
   - 提示词分享：保留 3 个专题结构，但替换其中一个专题内容。
   - 新专题：新增“受试者/参与者列名兼容改造”，承载 MedReview 项目 subject ID 列名统一方案的专项 Prompt。
   - 项目文件清理：继续保留。
   - 全访视矩阵访视顺序调整：继续保留。

4. 视觉优化
   - 保持现有视觉语言与卡片结构不变，仅调整专题顺序与文案内容。

   ## [MODIFY] walkthrough.md

计划内容：

   - 记录本轮专题替换内容。
   - 说明默认专题与子导航顺序变更。
   - 记录验证方式。

# 验证计划

自动验证：

- 检查 HTML 结构是否完整，避免标签嵌套错误。
- 检查导航点击是否能正确切换“首页 / 提示词分享”两个顶层页面。
- 检查新的首个专题是否默认打开，且“提示词分享”页内多个子层级都能正确定位和高亮。
- 检查移动端宽度下导航是否不会遮挡正文。

手动验证：

- 打开 [index.html](index.html)，确认侧栏导航可见。
- 依次点击“首页 / 提示词分享”，确认内容切换正确。
- 在“提示词分享”中检查新的 MedReview 专题是否位于第一位，且内容块与提示词代码块显示正常。
- 检查首页新增的维护说明、更新说明和页脚信息是否显示正常。
- 检查首页中的 4 张配图显示是否正常。
- 检查页面在深色模式和浅色模式下观感是否可接受。