# 变更概述

本轮在现有 Git 方法栏目基础上继续增强视频页的可用性。核心目标有两件事：

1. 给 Git 视频补充页面内封面图。
2. 在视频下方增加“关键命令摘要”，让用户不看完整视频也能快速回忆操作路径。

独立变更如下：

1. [MODIFY] index.html
   - 为 Git 视频增加 poster 封面图。
   - 在视频下方增加“关键命令摘要”卡片。
   - 保持 Git 页面原有说明文案和视频播放器结构。
   - 保持现有单文件结构、现有视觉语言和原生 JavaScript 切换逻辑。

2. [NEW] git方法-cover.svg
   - 新增 Git 视频封面图文件，供 video 的 poster 使用。

2. [MODIFY] walkthrough.md
   - 在完成实现后，同步记录本轮删除的专题与新增的方法 section。

# 理解摘要

- 当前项目是一个单文件静态文档站，主体在 [index.html](index.html)。
- 当前页面已经有“Git 方法”栏目，并已接入 [git方法.mp4](git方法.mp4)。
- 你现在进一步要求做两项增强：补封面图，以及补关键命令摘要。
- 这两个增强都只针对 Git 视频页，不影响其他栏目结构。

# 假设

- 假设仍然只维护 [index.html](index.html) 一个交付文件，不新增框架和构建流程。
- 假设封面图采用本地静态文件形式，和视频同级存放即可。
- 假设关键命令摘要以页面内静态说明卡片呈现，不需要交互展开。
- 假设你希望保留当前视觉风格，不做大规模 UI 重做，只做信息架构上的增删。

# 决策记录

- 已确认“全访视矩阵调整”专题应被完整移除，而不是仅隐藏入口。
- 已确认本轮要做的是 Git 页的增强，而不是新增栏目。
- 已确认需要同时做封面图和关键命令摘要两项内容。

# 提议变更

## [MODIFY] index.html

计划修改点：

1. 导航结构
   - 顶层导航结构不变，继续保持：首页、Git 方法、AI 协作经验库、AI Prompt 模板库。

2. Git 方法页内容
   - 为 [git方法.mp4](git方法.mp4) 增加 `poster` 封面图。
   - 在播放器下方增加“关键命令摘要”卡片，列出最基础的 Git 存档命令和用途说明。
   - 保持内容为单篇视频页，不额外增加页内子导航。

4. 路由与交互
   - 保持现有原生 JavaScript 页签切换逻辑。
   - 不改现有 `#git` 路由逻辑。
   - 保持现有 prompts / methods 页面逻辑不受影响。

## [MODIFY] walkthrough.md

计划内容：

- 记录 Git 视频封面图新增结果。
- 记录关键命令摘要的页面结构与验证结果。

# 验证计划

自动验证：

- 检查 HTML 结构是否完整，避免新增页面后出现未闭合标签。
- 检查 `poster` 是否正确指向 [git方法-cover.svg](git方法-cover.svg)。
- 检查关键命令摘要卡片是否完整显示。
- 检查视频标签路径是否正确指向 [git方法.mp4](git方法.mp4)。

手动验证：

- 打开 [index.html](index.html)，进入“Git 方法”，确认视频未播放前能看到封面图。
- 点击播放 [git方法.mp4](git方法.mp4)，确认浏览器可正常加载和控制播放。
- 检查视频下方“关键命令摘要”卡片内容是否完整、排版是否清晰。
- 检查桌面端和窄屏下的视频区域是否仍保持当前文档站风格。